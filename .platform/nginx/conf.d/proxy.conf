client_max_body_size 5M;

proxy_connect_timeout 10s;
proxy_read_timeout    30s;
proxy_send_timeout    30s;

# Gzip is handled by the Go app (middleware.Gzip),
# disable nginx gzip to avoid double-compression.
gzip off;
